# Prometheus Development Environment Credentials Template
# SECURITY WARNING: Copy this file to configs/credentials.env and change ALL default values!
# 
# DO NOT commit credentials.env file to version control
# DO NOT use these example values in any environment

# Basic Authentication Credentials
# Generate strong passwords using: openssl rand -base64 32
PROM_ADMIN_USER=admin
PROM_ADMIN_PASS=CHANGE_ME_ADMIN_PASSWORD

PROM_USER_USER=prometheus  
PROM_USER_PASS=CHANGE_ME_USER_PASSWORD

PROM_VIEWER_USER=viewer
PROM_VIEWER_PASS=CHANGE_ME_VIEWER_PASSWORD

PROM_TEST_USER=test
PROM_TEST_PASS=CHANGE_ME_TEST_PASSWORD

# Bearer Token Authentication
# Generate using: openssl rand -hex 32
PROM_BEARER_TOKEN=CHANGE_ME_BEARER_TOKEN

# API Token Authentication  
# Generate using: uuidgen or openssl rand -hex 32
PROM_API_TOKEN=CHANGE_ME_API_TOKEN

# Additional Security Settings
NGINX_RATE_LIMIT=30r/s
NGINX_BURST_LIMIT=50

# Instructions:
# 1. Copy this file: cp configs/credentials.env.example configs/credentials.env
# 2. Generate secure passwords:
#    - Password: openssl rand -base64 32
#    - Token: openssl rand -hex 32
#    - UUID: uuidgen
# 3. Replace ALL CHANGE_ME values with secure ones
# 4. Set appropriate file permissions: chmod 600 configs/credentials.env
# 5. Never commit credentials.env to version control
